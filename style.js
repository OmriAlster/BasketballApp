import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    screen: {
      padding: 50,
      alignItems: 'center'
    },
    goalsList: {
      
    }
  });

export default styles;